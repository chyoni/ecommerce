spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: ecommerce # Spring Cloud Config 에서 관리하는 설정 파일을 읽는다. 이 방식이 주는 이점은, 설정값을 변경하고 애플리케이션을 다시 시작하지 않아도 설정값만 따로 관리하는 Config Server 를 바라보고 설정값을 유동적으로 가져올 수 있다.